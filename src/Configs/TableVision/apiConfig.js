const tenant_domain = 'https://api.tablevision.ai/'

export const tv_apiConfig = {
    userAuth: {
        loginUrl: `${tenant_domain}accounts/auth/`,
        tenantLoginUrl: `${tenant_domain}accounts/auth/profile/`
    }
}
